# Configuration file for actionlint
# This file defines custom runner labels for self-hosted runners

self-hosted-runner:
  # Custom runner labels used in this repository
  labels:
    - ubuntu-22.04-arm
    - windows-11-arm
