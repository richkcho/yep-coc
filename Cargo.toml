[package]
name = "yep-coc"
version = "0.1.0"
edition = "2024"

[workspace]
members = ["test_support"]

[dev-dependencies]
clap = { version = "4.4", features = ["derive"] }
test_support = {path = "test_support"}
